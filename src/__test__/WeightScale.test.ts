import { FileEncoder } from '../FileEncoder';
import ProtocolVersion from '../ProtocolVersion';

test('WeightScale', () => {
  const _encoder = new FileEncoder('./test.fit', ProtocolVersion.V1_0);
  void _encoder;
});
